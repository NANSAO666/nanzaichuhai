# 南崽出海 - uniCloud云端同步配置指南

## 准备工作
- 账号：13003192344
- 服务空间：uber-nannnan（阿里云）
- SpaceID: mp-be8bb2ea-2253-4636-aef9-ad4968f829d4

---

## 第一步：创建数据库表（2张）

1. 打开 https://unicloud.dcloud.net.cn 登录
2. 进入 `uber-nannnan` 服务空间
3. 左侧菜单 → 云数据库 → 云数据库
4. 点击「新建」按钮

### 表1：nzch_config（配置表）
- 表名：`nzch_config`
- 权限：所有用户可读，仅创建者可读写（或按默认）
- 点击创建

### 表2：nzch_orders（订单表）
- 表名：`nzch_orders`
- 权限：所有用户可读，仅创建者可读写（或按默认）
- 点击创建

---

## 第二步：创建云函数

1. 左侧菜单 → 云函数/云对象 → 函数/对象列表
2. 点击「新建云函数」
3. 函数名：`nzch-api`
4. 点击创建

### 上传云函数代码

**方式一：在线编辑（推荐）**
1. 找到刚创建的 `nzch-api`，点击「详情」
2. 点击「在线编辑」
3. 把下面的代码全部复制，替换掉默认的 `index.js` 内容
4. 按 Ctrl+S 保存

**方式二：本地上传**
1. 用HBuilderX创建云函数
2. 把 `/workspace/unicloud/nzch-api/index.js` 的内容复制过去
3. 右键上传部署

### 云函数完整代码

```javascript
'use strict';

const db = uniCloud.database();
const configCollection = db.collection('nzch_config');
const ordersCollection = db.collection('nzch_orders');

const ADMIN_TOKEN = 'nzch_admin_2026';

exports.main = async (event, context) => {
  const { action, data, token } = event;
  
  try {
    switch(action) {
      case 'getConfig':
        return await getConfig();
      case 'updateConfig':
        if(token !== ADMIN_TOKEN) return { errCode: 403, errMsg: '无权限' };
        return await updateConfig(data);
      case 'createOrder':
        return await createOrder(data);
      case 'getOrders':
        if(token !== ADMIN_TOKEN) return { errCode: 403, errMsg: '无权限' };
        return await getOrders(data);
      case 'updateOrder':
        if(token !== ADMIN_TOKEN) return { errCode: 403, errMsg: '无权限' };
        return await updateOrder(data);
      default:
        return { errCode: 400, errMsg: '未知操作' };
    }
  } catch(e) {
    console.error('API错误:', e);
    return { errCode: 500, errMsg: e.message };
  }
};

async function getConfig() {
  const res = await configCollection.where({ key: 'main' }).get();
  if(res.data && res.data.length > 0) {
    return { errCode: 0, errMsg: '', data: res.data[0].value };
  }
  return { errCode: 0, errMsg: '', data: null };
}

async function updateConfig(configData) {
  const res = await configCollection.where({ key: 'main' }).get();
  if(res.data && res.data.length > 0) {
    await configCollection.doc(res.data[0]._id).update({
      value: configData,
      updateTime: Date.now()
    });
  } else {
    await configCollection.add({
      key: 'main',
      value: configData,
      createTime: Date.now(),
      updateTime: Date.now()
    });
  }
  return { errCode: 0, errMsg: '', data: { success: true } };
}

async function createOrder(order) {
  const result = await ordersCollection.add({
    ...order,
    createTime: Date.now(),
    updateTime: Date.now()
  });
  return { 
    errCode: 0, 
    errMsg: '', 
    data: { 
      id: result.id,
      orderId: order.orderId
    } 
  };
}

async function getOrders(params = {}) {
  const { phone, status, clubId, page = 1, pageSize = 50 } = params;
  let query = ordersCollection;
  
  const where = {};
  if(phone) where.customerPhone = phone;
  if(status) where.status = status;
  if(clubId) where.clubId = clubId;
  
  if(Object.keys(where).length > 0) {
    query = query.where(where);
  }
  
  const countRes = await query.count();
  const listRes = await query
    .orderBy('createTime', 'desc')
    .skip((page - 1) * pageSize)
    .limit(pageSize)
    .get();
  
  return {
    errCode: 0,
    errMsg: '',
    data: {
      list: listRes.data,
      total: countRes.total,
      page,
      pageSize
    }
  };
}

async function updateOrder(params) {
  const { orderId, status, ...updateData } = params;
  const res = await ordersCollection.where({ orderId }).get();
  
  if(!res.data || res.data.length === 0) {
    return { errCode: 404, errMsg: '订单不存在' };
  }
  
  await ordersCollection.doc(res.data[0]._id).update({
    ...updateData,
    status: status || res.data[0].status,
    updateTime: Date.now()
  });
  
  return { errCode: 0, errMsg: '', data: { success: true } };
}
```

---

## 第三步：设置云函数URL化

1. 在 `nzch-api` 云函数详情页
2. 找到「云函数 URL 化」或「HTTP 访问路径」
3. 点击「开启」或「添加路径」
4. 路径随便填，比如：`/api`
5. 开启后会得到一个URL地址，类似：
   `https://xxx.bspapp.com/http/api`
6. **把这个URL复制保存好！** 后面要用

---

## 第四步：配置管理员后台

1. 打开管理员后台：https://nansao666.github.io/nanzaichuhai/管理员后台.html
2. 按 F12 打开控制台
3. 输入以下代码（把URL换成你刚才复制的）：

```javascript
StorageModule.set('nzch_cloud_url', '你的云函数URL地址');
location.reload();
```

4. 页面刷新后，配置会自动同步到云端
5. 以后每次保存配置，都会自动同步到云端

---

## 第五步：配置客户前端

有两种方式：

### 方式A：通过链接参数（推荐）
管理员后台生成客户链接时自动带上云端地址：
- 客户打开链接后自动配置云端同步
- 链接格式：`客户前端.html?cloud=你的云函数URL`

### 方式B：手动配置
在客户前端按F12执行：
```javascript
StorageModule.set('nzch_cloud_url', '你的云函数URL地址');
location.reload();
```

---

## 验证是否成功

### 验证配置同步
1. 在管理员后台修改品牌名称，保存
2. 打开客户前端，刷新页面
3. 看看品牌名称是不是更新了 → **是就成功了！**

### 验证订单同步
1. 在客户前端提交一个预约
2. 刷新管理员后台的订单台账
3. 看看新订单是不是出现了 → **是就成功了！**

---

## 注意事项

1. **管理员Token**：默认是 `nzch_admin_2026`，如果要改，云函数和前端都要一起改
2. **数据备份**：重要数据建议定期导出备份
3. **容量限制**：免费版有容量限制，图片太多可能会超，可以用云存储存图片
4. **微信推送**：云端模式下，推送可以在云函数里做（后续可扩展）

---

## 遇到问题怎么办？

1. 先看云函数日志：uniCloud控制台 → 云函数 → nzch-api → 日志
2. 浏览器按F12看控制台有没有报错
3. 检查URL地址对不对，有没有复制完整
4. 检查表名是不是 `nzch_config` 和 `nzch_orders`，别写错
